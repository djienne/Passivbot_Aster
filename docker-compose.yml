services:
  passivbot-live:
    build:
      context: .
      dockerfile: Dockerfile_live
    image: passivbot-live:latest
    container_name: passivbot-live
    restart: unless-stopped
    volumes:
      - ./:/app/
    working_dir: /app
    environment:
      PYTHONUNBUFFERED: "1"
      PASSIVBOT_CONFIG: "configs/config_hype_aster.json"
      PASSIVBOT_ARGS: ""
    command:
      - sh
      - -lc
      - python src/main.py "$${PASSIVBOT_CONFIG}" $${PASSIVBOT_ARGS}
    deploy:
      resources:
        limits:
          memory: 512M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "kill -0 1"]
      interval: 30s
      timeout: 5s
      retries: 3
